extends main

block content
  .flex-container
    .row
      ul.col.s12
        each post in posts
          li  
            a(href=`/posts/${post.id}`)
              h1 #{post.title}
            h3 #{post.category}
            p #{post.body}


          